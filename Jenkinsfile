parameters {
  string(
    name: 'YOURNAME', 
    defaultValue: 'world', 
    description: 'Name to greet'
  )
}

timestamps {

    stage('Say Hello') {
        echo "Hello ${env.YOURNAME}.  Everything is awesome!"
     }
}
